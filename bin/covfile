#! /bin/bash

if [ $# -eq 0 ]; then
    echo "covfile prints coverage information for a given file by a test"
    echo ""
    echo "No arguments provided."
    echo "Usage: covfile TestName file_name.go"
    exit 1
fi

set -x
go-carpet -256colors -file "$2" -args "--run $1" | bat
